{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "chrome",
            "request": "attach",
            "name": "Chrome?p=9000",
            "url": "http://localhost:9000",
            "webRoot": "${workspaceRoot}"
        }
    ]
}